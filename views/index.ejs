<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/index.css">
    <title>Document</title>
</head>
<body>
    <h1>Lista de usuarios</h1>
    <table>
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Email</th>
            <th>Acciones</th>
        </tr>
        <!-- JS -->
        <% users.forEach(user=>{ %>
        <tr>
            <td><%=user.id %></td>
            <td><%=user.nombre %></td>
            <td><%=user.email %></td>
            <td>
                <a href="/edit/<%= user.id %>">Editar</a>|
                <a href="/delete/<%= user.id %>">Eliminar</a>
            </td>
        </tr>
        <% }); %>
        <div class="agregar">
            <button onclick="location.href='/add'">Agregar</button>
        </div>
    </table>
</body>
</html>